define([ ], function() {
/* eslint-enable max-statements */
    var baseUrl = 'file:///scaffold-www/error-views/';

    var errors = {
        connectivityError: {
            url: baseUrl + 'connectivity-error.html',
        }
    };
    return {
        errors: errors
    };
});
