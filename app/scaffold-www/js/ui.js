// Scripts required in via this file will be combined into ui.js
require(
    [
        'left-drawer'
    ],
    function(
        leftDrawerUI
    ) {
        // Noop for now
    },
    null,
    true // force sync
);
